---
- name: Setup NGINX
  hosts: localhost
  tasks: 
    - name: Install NGINX
      dnf:
        name: 
          - nginx
        state: present
    
    - name: Enable and start NGINX service
      systemd:
        name: nginx
        enabled: yes
        state: started


